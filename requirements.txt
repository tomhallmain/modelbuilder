# Core + unified GUI (PySide6 / Qt for Python). Single install: pip install -r requirements.txt
Pillow>=9.0.0
numpy>=1.25.2,<2.0  # SciPy/TensorFlow stack; TensorFlow expects NumPy < 2.0
pyyaml>=6.0

# Encryption (utils/encryptor.py, app_info_cache encrypted store)
cryptography>=41.0.0
keyring>=24.0.0

# GUI (unified with CLI)
PySide6>=6.5.0

# Framework dependencies (install as needed)
# PyTorch:
# torch>=2.0.0
# torchvision>=0.15.0

# Keras/TensorFlow:
# tensorflow>=2.10.0

# Optional: For model conversion
# onnx>=1.12.0
# onnx-tf>=1.10.0

# Legacy dependencies (kept for existing scripts, not required for new mb CLI)
# fastai>=2.0.0
# ipython>=7.0.0
# pathlib2>=2.3.0; python_version < "3.4"
